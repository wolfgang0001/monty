#include "monty.h"

/**
 * queue - adding a new new node to the queue.
 * @stack: head of the stack
 * @line_number: file's line
 * Return: nothing
 */

void queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	bus.status = QUEUE;
}
